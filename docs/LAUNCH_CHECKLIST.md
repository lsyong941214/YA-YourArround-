# 서비스 출시 체크리스트 (앱인토스 개발자센터 기준)

출처: 앱인토스 개발자센터 "서비스 출시 체크리스트" 스크린샷 (2026-09-18 공유).
지역 기반 매칭 서비스 특성상 신뢰·안전 항목 비중이 높아, 원문 11개 항목을 이 저장소
기준으로 상태 점검 후 실제 개발 TODO로 풀어서 정리한다.

상태 표기: ✅ 구현됨 / 🔶 부분 구현 / ❌ 미구현(개발 필요) / 📄 비개발(법무·운영 항목)

## 1. 법인 등록이 완료됐어요
- 📄 비개발 — 코드로 확인/구현할 수 없는 사업자 등록 항목. 별도로 확인 필요.

## 2. 허위 프로필·도용 사진을 차단하기 위한 AI·운영·수동 검증 프로세스
- ❌ 미구현 — 현재 온보딩(`/onbd`)/프로필 수정(`ProfEditModal`)은 업로드된 사진을
  Storage에 저장만 하고 아무 검증도 하지 않는다.
- TODO
  - [ ] 프로필/앨범 사진 신고 사유에 "허위 프로필·도용 사진" 항목 추가 (아래 4번과 연계)
  - [ ] 신고 누적 시 관리자 검토 큐에 노출되는 최소 운영 프로세스 마련
  - [ ] (후속) AI 이미지 검증 API 연동은 별도 과제로 분리

## 3. 앱 내 불법 광고(조건 만남, 성매매 등) 탐지·차단 시스템
- 🔶 부분 구현 (2026-09-18) — `src/lib/text_filt.ts`에 키워드 기반 1차 필터 추가, 온보딩
  소개글(`OnbdScreen`)/프로필 수정 소개글(`ProfEditModal`)/채팅 메시지(`chat_store.send_msg`)
  전송 시 적용해 걸리면 저장·전송을 막고 안내 문구를 보여준다.
- TODO (남은 것)
  - [ ] 필터에 걸린 시도를 신고 큐로 자동 연계(반복 시도 계정 자동 표시)
  - [ ] 키워드 목록을 코드 배포 없이 갱신할 수 있는 구조(DB 테이블 등)로 전환 — 지금은
        `text_filt.ts`에 하드코딩

## 4. 사용자 신고 기능(원클릭 신고, 차단)
- 🔶 부분 구현 (2026-09-18) — `reports`/`user_blocks` 테이블 + RLS 추가
  (`supabase/schema.sql`, 기존 프로젝트용 `supabase/alter_rept_blck.sql`), 신고/차단
  저장소(`src/lib/store/safe_store.ts`), 프로필 보기 팝업(`ProfileViewModal`)에 "신고하기"
  (사유 선택 `ReportModal`)/"차단하기" 버튼 추가. 차단하면 연락처 목록(`cntc_store.list_chf_of`/
  `list_res_of`)에서 상대가 즉시 빠진다.
- TODO (남은 것)
  - [ ] 채팅 화면(`ChatScreen`)에도 신고/차단 진입점 추가 (지금은 프로필 보기 팝업 경유만 가능)
  - [ ] 차단이 편도(내가 차단 = 나만 상대를 못 봄)라, 상대에게도 내가 안 보이게 하려면
        RLS를 우회하는 조회 경로(뷰/함수)가 필요 — 현재 RLS상 서로의 `user_blocks` 행은
        조회 불가라 양방향 차단은 후속 작업
  - [ ] 매칭 추천 후보/주변인 테스트 요청 대상 목록에도 차단 필터 반영 (지금은 연락처
        목록만 필터링됨)

## 5. 신고 접수 후 24시간 이내 대응 프로세스
- 🔶 부분 구현 (2026-09-19) — 신고 처리 화면 `/admin`(`AdminScreen.tsx`,
  `src/lib/store/admin_store.ts`) 추가. 대기중(open) 신고를 접수 오래된 순으로 보여주고,
  접수 후 24시간이 지난 건은 "24시간 초과" 배지로 상단 요약 + 카드에 강조 표시한다.
  관리자는 각 신고를 처리중/완료로 바꾸고, 대상 계정을 바로 정지/영구차단/정지해제할 수
  있다. `profiles.is_admin`이 true인 계정만 들어갈 수 있고(`supabase/alter_admin.sql`),
  앱 메뉴 어디에도 진입 링크가 없어 URL(`/admin`)로만 접근한다.
- TODO (남은 것)
  - [ ] "24시간 이내"를 실제로 강제하는 건 사람(운영자)이 화면을 들여다보는 것뿐 — 초과
        건을 이메일/슬랙 등으로 알려주는 자동 알림은 아직 없음
  - [ ] 처리 이력(누가 언제 상태를 바꿨는지) 감사 로그가 없음 — 지금은 `reports.status`
        최종 값만 남는다

## 6. 반복 위반자를 영구 차단하는 정책
- 🔶 부분 구현 (2026-09-19) — `profiles.acct_stat`('actv'/'susp'/'ban') 컬럼 추가
  (`supabase/schema.sql`, 기존 프로젝트용 `supabase/alter_acct_stat.sql`). 서로 다른
  신고자 3명 이상이 같은 대상을 신고하면(`reports.status = 'open'`) `reports_auto_susp`
  트리거가 자동으로 `susp`(정지)로 전환한다. `sess_stat()`/`curr_user()`
  (`src/lib/store/auth_store.ts`)가 `actv`가 아닌 계정을 걸러내고, 로그인 직후
  안내 화면(`/suspended`, `SuspScreen.tsx`)으로 보낸다 — 정지든 영구 차단이든 그 계정으로는
  더 이상 앱을 쓸 수 없다. 연락처 목록(`cntc_store`)에서도 상대가 `actv`가 아니면 제외.
- TODO (남은 것)
  - [x] ~~`susp`(정지) → `ban`(영구 차단) 전환은 관리자 화면이 없어 아직 수동~~ (2026-09-19,
        아래 5번의 `/admin` 화면에서 버튼으로 처리 가능해짐)
  - [x] ~~`susp`에서 다시 `actv`로 되돌리는(정지 해제) 절차/화면도 아직 없음~~ (2026-09-19,
        `/admin`의 "대상 정지 해제" 버튼)
  - [ ] 이미 세션이 열려 있는 상태(로그인된 채로 브라우저 탭을 켜둔 경우)에는 다음 로그인
        전까지 정지가 즉시 반영되지 않음 — `curr_user()`가 홈 등 각 화면에서 호출될 때마다
        `actv`를 재확인하긴 하지만(그 시점부턴 `null` 반환 → `/login`행), 이미 메모리에
        올라온 화면 상태 자체를 강제로 끊는 장치는 없음

## 7. 대화 내용을 저장할 수 있는 시스템
- ✅ 구현됨 — `public.chat_messages` 테이블 + Supabase Realtime (`v0.3.x`, README
  "메시징(채팅) 시스템" 절 참고). 당사자 외에는 이장도 조회 불가(RLS).
- TODO (보완)
  - [ ] 신고/수사기관 협조(10번) 목적의 보존기간 정책 정의, 필요시 관리자 조회 경로 마련
        (현재 RLS는 신고 대응을 위한 예외 조회 경로가 없음)

## 8. 민감정보(성적 지향, 위치 등)는 최소한으로 수집
- 🔶 부분 구현 — 현재 수집 항목은 이름/역할/생년월일/MBTI/직업/지역(텍스트)/소개/사진뿐,
  성적 지향·정밀 위치(GPS) 등은 수집하지 않음.
- TODO
  - [ ] `profiles.region`(지역) 자유 텍스트 입력이 과도하게 상세한 주소로 이어지지
        않도록 입력 가이드/행정동 단위 제한 검토
  - [ ] 개인정보 수집 항목 목록을 별도 문서(개인정보처리방침)로 정리해 9번과 함께 관리

## 9. 청소년 보호법, 개인정보보호법 등 관련 법령 준수
- 🔶 부분 구현 (2026-09-18) — 온보딩(`OnbdScreen`)에서 `calc_age(birth_dt)`로 만 19세 미만
  가입을 클라이언트에서 차단(안내 문구 표시). 이용약관/개인정보처리방침 동의 화면은 아직 없음.
- TODO (남은 것)
  - [ ] 연령 검증은 현재 클라이언트(온보딩 화면)에만 있음 — `make_prof()`/DB 쪽에도 서버
        검증(트리거 또는 CHECK)을 추가해 API를 직접 호출하는 우회를 막을 필요
  - [ ] 회원가입 단계에 이용약관·개인정보처리방침 동의 체크박스 + 문서 화면 추가
  - [ ] 개인정보처리방침 문서 작성은 법무 검토 필요 (📄 비개발 영역과 겹침)

## 10. 불법 행위 발생 시 수사기관에 협조할 수 있는 체계
- 🔶 부분 구현 (2026-09-19) — 5번의 `/admin` 화면에서 특정 계정에 대한 신고 이력(신고자/
  사유/상세/처리 상태)과 현재 계정 상태(정상/정지/영구차단)를 관리자가 한 화면에서 확인할
  수 있다. 채팅 데이터(7번)는 여전히 DB에 원본으로 남아있지만, `/admin`은 신고·계정 상태만
  다루고 채팅 내용 자체는 화면에 노출하지 않는다 — 대화 내용은 민감도가 더 높아서, 넓은
  RLS 우회 없이 신고 처리 화면에 얹기보다는 스코프를 분리해뒀다.
- TODO (남은 것)
  - [ ] 수사기관 요청으로 채팅 원문 자체를 조회해야 하는 경우는 아직 화면이 없음 — 지금은
        Supabase SQL Editor에서 `match_request_id`/기간으로 `chat_messages`를 직접 조회
        (RLS가 당사자 외 조회를 막으므로 서비스 롤 키로만 가능 — 반드시 관리자 신원 확인
        후, 요청 건별로만 실행할 것)
  - [ ] 데이터 제공 절차(요청 접수 → 신원 확인 → 범위 내 제공) 운영 문서화 (📄 비개발)

## 11. 유료 결제 환불 및 분쟁 해결 절차
- ❌ 미구현 — 현재 앱에 결제 기능 자체가 없음(포인트 UI는 홈 화면에 표시만 되고 실제
  과금 로직 없음).
- TODO
  - [ ] 결제 기능 도입 전, 환불 정책·분쟁 해결 절차 문서 우선 정의 (📄 비개발)
  - [ ] 결제 기능 구현 시 환불 요청 화면/상태 관리를 함께 설계 (결제 자체가 미착수이므로
        후순위)

## 우선순위 제안 (개발 항목만)
1. ~~**4번 (신고/차단 MVP)**~~ (2026-09-18 구현) — 채팅 화면 진입점 추가, 양방향 차단,
   추천/테스트 후보 목록 필터링은 남음
2. ~~**9번 (연령 검증)**~~ (2026-09-18 구현, 클라이언트만) / 약관 동의 화면은 남음
3. ~~**3번 (금칙어 필터링)**~~ (2026-09-18 구현)
4. ~~**6번 (계정 영구 정지)**~~ (2026-09-19 구현) — `/admin`에서 처리 가능해짐
5. ~~**5·10번 (운영 대응 체계)**~~ (2026-09-19 구현, `/admin`) — 24시간 초과 알림 자동화,
   처리 감사 로그, 채팅 원문 조회 화면은 남음
6. **2·11번** — 후순위 (AI 검증, 결제 미도입 상태)
